version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: quickshop_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: quickshop_user
      POSTGRES_PASSWORD: quickshop_pass
      POSTGRES_DB: quickshop_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U quickshop_user -d quickshop_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quickshop_web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://quickshop_user:quickshop_pass@db:5432/quickshop_db?schema=public"
      NEXTAUTH_SECRET: "${NEXTAUTH_SECRET:-change-me-in-production}"
      NEXTAUTH_URL: "${NEXTAUTH_URL:-http://localhost:3000}"
      SUPER_ADMIN_EMAIL: "${SUPER_ADMIN_EMAIL:-itadmit@gmail.com}"
      SUPER_ADMIN_PASSWORD: "${SUPER_ADMIN_PASSWORD:-115599}"
    volumes:
      - uploads_data:/app/public/uploads
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
    driver: local
  uploads_data:
    driver: local


